{
  "name": "smart-safety-band-backend",
  "version": "1.0.0",
  "description": "Backend API for Smart Safety Band application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd ../client && npm run dev",
    "server": "nodemon server.js",
    "build": "cd ../client && npm run build",
    "seed": "node seedDatabase.js",
    "heroku-postbuild": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}